<a href="<%= root_path %>" class="mx-auto text-2xl md:text-3xl font-bold items-center flex space-x-3">
  <%# CCN fork: the account's logo when it has one, else upstream's mark (specs/003-p1-features, US2) %>
  <%= render 'shared/ccn_brand_logo', class: 'w-9 h-9 md:w-12 md:h-12', logo_class: 'max-h-9 md:max-h-12 max-w-xs object-contain' %>
  <span><%= Docuseal.product_name %></span>
</a>
