# CCN fork — change register

Fork of [docusealco/docuseal](https://github.com/docusealco/docuseal) (AGPL-3.0 + `LICENSE_ADDITIONAL_TERMS`) maintained by the Communauté du Chemin Neuf for
https://docuseal.cheminneuf.community. Plan and rationale: `FORK-PLAN.md` in the private ops folder (`~/Projets_apps_github/DocuSeal`).

Branches: `master` mirrors upstream tags untouched · `ccn` = this patch stack, rebased onto each upstream tag.
Release tags: `<upstream>-ccn.<n>` (e.g. `3.2.4-ccn.0`) → image `ghcr.io/lucouto/docuseal-ccn:<tag>`.

## Rules (see FORK-PLAN.md §6)

1. New files first; fill upstream's empty hook partials second; edit upstream files last and minimally.
2. One feature = one commit (or a short series). Every upstream file touched is listed below — this table is the rebase checklist.
3. Migrations additive only: the stock `docuseal/docuseal:<upstream>` image must still boot on our schema.
4. Never modify signing / result generation / audit-trail code.
5. Keep DocuSeal attribution in the UI (AGPL §7(b) additional term) and keep this repository public (AGPL §13).

## Rebase procedure

```
git fetch upstream --tags
git rebase --onto <new-tag> <old-tag> ccn        # resolve using the table below
# CI green → tag <new-tag>-ccn.1 → image builds → staging from prod snapshot → promote
```

## Upstream files touched on `ccn`

| Upstream file | Feature / commit | Why |
|---------------|------------------|-----|
| `Gemfile`, `Gemfile.lock` | Stage 1 — formulas; Stage 4 — reminders | adds `dentaku` (MIT): server-side counterpart of the client calculator (a JS port of Dentaku); Stage 4 adds `sidekiq-cron` 2.4.0 (LGPL-3.0), pinned: the reminder sweep needs a scheduler and the app has none. Loaded only when `CCN_REMINDERS_ENABLED=true` *and* `Sidekiq.server?`; its web UI panel is deliberately not mounted |
| `lib/submitters/submit_values.rb` | Stage 1 — formulas | replaces the two Pro stubs `calculate_formula_value` (returned 0) and `eval_text_formula_value` (returned '') |
| `app/views/templates/edit.html.erb` | Stage 1 — builder | `data-with-conditions`, `data-with-formula` |
| `app/javascript/template_builder/fields.vue` | Stage 1 — de-Pro | locked "phone" upsell tile (→ docuseal.com/pricing) removed |
| `app/javascript/submission_form/formula_areas.vue` | Stage 1 — formulas | referenced values coerced to numbers (`numericFormulaValue`) with the same rule as the server |
| `config/application.rb` | Stage 1 — i18n | `config.i18n.load_path += config/locales/ccn/**/*.yml` (after upstream's file, so keys can be overridden) |
| `app/views/shared/_settings_nav.html.erb` | Stage 1 — de-Pro | Plans/Console entries multitenant-only; SSO/SMS behind `Ccn::SSO_ENABLED`/`SMS_ENABLED`; version badge → fork release |
| `app/views/shared/_navbar_buttons.html.erb` | Stage 1 — de-Pro | "Upgrade" button on /settings removed |
| `app/views/shared/_navbar.html.erb` | Stage 1 — de-Pro | user-menu "Console" entry (→ console.docuseal.com) multitenant-only |
| `app/views/templates_preferences/show.html.erb`, `app/views/templates_code_modal/show.html.erb` | Stage 1 — de-Pro | `templates/embedding` snippets behind `Ccn::EMBEDDING_ENABLED` (embed script is a stub here; snippets link to the cloud console) |
| `app/views/shared/_powered_by.html.erb` | Stage 1 — AGPL §13 | adds the source-code link next to the DocuSeal attribution (attribution kept, §7(b)) |
| `app/views/users/_role_select.html.erb` | Stage 1 — de-Pro; Stage 4 — roles | upsell link removed; Stage 4 drops `disabled` from the editor and viewer options, which `lib/ability.rb` now implements, and builds them as a list instead of a block — a block reaches `options_for_select` as a String, which returns it untouched, so no option was ever marked `selected` and the form saved back the first one. The user's own role is added to the list when it is not one of the three (`integration`) |
| `app/views/sso_settings/_placeholder.html.erb`, `sms_settings/_placeholder.html.erb`, `templates_code_modal/_placeholder.html.erb` | Stage 1 — de-Pro | render `shared/ccn_not_yet` |
| `app/views/submissions/_send_sms_button.html.erb`, `app/views/esign_settings/_default_signature_row.html.erb` | Stage 1 — de-Pro | emptied (SMS is P2; the AATL row is DocuSeal's own certificate) |
| hook partials `personalization_settings/_logo_form`, `notifications_settings/_reminder_banner`, `submissions/_list_form` | Stage 1 — de-Pro; Stage 4 — filled | rendered `shared/ccn_not_yet` until Stage 4 filled all three: the logo form (US2), the reminder e-mail template form (US1) and the Upload list tab (US4). Each has its own row below |
| `lib/templates/create_attachments.rb` | Stage 2 — ingestion | `handle_file_types`: office documents → `Ccn::OfficeDocument.convert` (Gotenberg) → PDF path; `handle_pdf_or_image`: link annotations collected first, then `Ccn::TextTags.call` before the blob is created (tags → fields, erased unless `remove_tags=false`; erased pages are stored flattened), document uuid pre-generated, `ProcessDocument.call(extract_fields: false)` for tagged documents; `create_document(…, uuid:)` |
| `lib/templates/process_document.rb`, `lib/templates/replace_attachments.rb` | Stage 2 — ingestion | one line each: `f['submitter_uuid'] ||= …` so a submitter assigned from a tag role survives `normalize_attachment_fields` |
| `app/controllers/application_controller.rb` | Stage 2 — ingestion | `rescue_from Ccn::Gotenberg::Error` → JSON 422 / redirect with the translated message |
| `app/controllers/templates_uploads_controller.rb` | Stage 2 — ingestion | conversion errors skip the generic rescue (template just created is destroyed → no partial template; the translated alert is shown) |
| `app/controllers/template_documents_controller.rb` | Stage 2 — ingestion | submitters added by tag roles are saved and returned (`submitters:`) so the builder shows them |
| `app/javascript/template_builder/builder.vue` | Stage 2 — ingestion | one line: a detected field keeps the `submitter_uuid` its tag role assigned (was overwritten with the selected submitter) |
| `lib/docuseal.rb` | Stage 2 — ingestion | `advanced_formats?` → also true when `GOTENBERG_URL` is set (drop zone / upload button accept DOCX etc.) |
| `config/routes.rb` | Stage 2 — API; Stage 3 — admin API; Stage 4 — reminders, logo | inside `namespace :api`: `POST templates/{pdf,docx,doc,html,merge}`, `PUT templates/:id/documents` → `Api::CcnTemplatesDocumentsController`; `POST submissions/{pdf,docx,html}` → `Api::CcnSubmissionsDocumentsController`; Stage 3: one `scope 'ccn'` block (users, webhooks + secret/events/resend/test, account_configs, template_folders, `scope 'templates/:template_id'` → versions + restore, detect_fields) — flat `Api::Ccn*` controllers, `as: 'ccn_…'` helpers (a `resources … as:` would rename the nested param, hence the scope); Stage 4: `reminders/due`, `reminders/run` and `account_logo` (GET/PUT/DELETE) in that same `scope 'ccn'`, plus `resource :personalization_logo` in the settings scope for the upload form and `resources :submissions_lists` (with `post :preview`) nested in `resources :templates` for the Upload list tab |
| `app/controllers/api/templates_controller.rb` | Stage 3 — `preferences{}` on `PUT /api/templates/{id}` (specs/002 FR-004) | one line: `include Ccn::TemplatePreferences::ApiHook` after `load_and_authorize_resource` — the concern adds a `before_action` (only `update`) that applies the UI's preference allow-list and coercions to `@template` before upstream's `update!`, and a `rescue_from Ccn::AdminInvalid` → 422. Upstream's `update` body is untouched (its ABC size is at the rubocop limit) |
| `app/controllers/mcp_controller.rb` | Stage 3 — MCP tools (specs/002 FR-009) | eight entries in `TOOL_CONTROLLERS` (`create_template_from_documents`, `update_template_documents`, `merge_templates`, `create_submission_from_documents`, `manage_users`, `manage_webhooks`, `account_config`, `set_template_preferences`) → `Mcp::Ccn*Controller`; `TOOLS` derives from them unchanged |
| `app/controllers/errors_controller.rb` | Stage 2 — API | `ENTERPRISE_PATHS` / `ENTERPRISE_FEATURE_MESSAGE` (Pro-only 404 for those paths) removed; the non-`/api` twins (`/templates/pdf`, `/submissions/html`, …) are not routed by the fork and now answer a plain 404 |
| `spec/requests/openapi_contract_spec.rb` | Stage 2 — API | all 8 Pro operations leave the pending list (now `[]`) and get conforming-response examples |
| `app/views/notifications_settings/_reminder_banner.html.erb`, `app/views/templates_preferences/_submitter_invitation_reminder_email_collapse.html.erb` | Stage 4 — reminders | the two empty Pro hooks filled in: the reminder e-mail template form (account) and its per-template override, bound to the `submitter_invitation_reminder_email` config and the `invitation_reminder_email_subject/body` preferences, both of which already existed |
| `app/models/account.rb` | Stage 4 — logo | one line: `include Ccn::AccountLogo` (`has_one_attached :logo` + its validation). No migration — the ActiveStorage tables are already there |
| `app/views/personalization_settings/_logo_form.html.erb` | Stage 4 — logo | the Pro hook filled in: upload, preview and removal, plus a warning when the instance has no absolute URL configured (the logo cannot then appear in e-mails) |
| `app/views/submit_form/_docuseal_logo.html.erb`, `app/views/start_form/_docuseal_logo.html.erb` | Stage 4 — logo | one render line each: `shared/_ccn_brand_logo` instead of `shared/_logo`, which falls back to the DocuSeal mark when the account has no logo. The attribution partials (`shared/_attribution`, `_powered_by`) are untouched — AGPL §7(b) is structural here, not a rule to remember |
| `app/views/layouts/mailer.html.erb` | Stage 4 — logo | one render line before `yield`: `shared/_ccn_mailer_logo`, which renders nothing without a logo or without an absolute URL. `shared/_mailer_attribution` below it is untouched |
| `app/models/user.rb` | Stage 4 — roles | two lines: `ROLES` gains `EDITOR_ROLE`/`VIEWER_ROLE` (`ADMIN_ROLE` unchanged and still the default), and `include Ccn::LastAdminGuard` so an account cannot be left without an administrator whatever path tries |
| `lib/ability.rb` | Stage 4 — roles | rewritten as three branches on `user.role` (research D9): admin — and the ad-hoc `integration`/`superadmin` — keep exactly the rules they had; editor gets documents and sending; viewer gets reads. Every role manages their own User record, because `ProfileController` asks for `manage` on it. Rewritten rather than appended to because the class had no role concept at all |
| `app/controllers/send_submission_email_controller.rb` | Stage 4 — logo | one line: the template is kept as `@template` instead of a local. Without it, the success page has no record to resolve the account from when no completed submitter matched the address typed, and loses the logo mid-flow |
| `app/controllers/users_controller.rb` | Stage 4 — roles | one line: `include Ccn::UsersControllerGuard`. `#destroy` archives with `update!`, so the last-admin guard firing there would have been a 500 instead of an alert |

## New files owned by the fork

| File | Purpose |
|------|---------|
| `.github/workflows/ccn-image.yml` | GHCR image build on `*-ccn.*` tags (linux/amd64) |
| `CCN-CHANGES.md` | this register |
| `lib/ccn.rb` | `Ccn` namespace: `SOURCE_URL`, `SSO_ENABLED`, `SMS_ENABLED`, `EMBEDDING_ENABLED`, `GOTENBERG_URL`, `PAGE_SIZES`, `DEFAULT_PAGE_SIZE` (env `CCN_DEFAULT_PAGE_SIZE`, default Letter), `NotSupportedYet` (moved from `config/initializers/zz_ccn.rb` in Stage 2 so the constants are reloadable and never referenced at initializer load time) |
| `lib/templates/find_text_tag_fields.rb` | text-tag detector: `{{Name;attr=…}}` (no brace inside) on `Page#text_nodes` lines (pdfium's own 4 pt/width grouping) → fields (+ transient role) and erase rectangles (`color: 'white'` = glyphs removed, nothing painted). First 200 pages only |
| `lib/ccn/text_tags.rb`, `lib/ccn/office_document.rb`, `lib/ccn/gotenberg.rb`, `lib/ccn/assign_roles.rb`, `lib/ccn/document_params.rb`, `lib/ccn/html_field_tags.rb` | Stage 2 ingestion: tag orchestration (detection guarded — a detector failure falls back to the upstream path) + erase (`save(flags: FPDF_REMOVE_SECURITY)` so a decrypted upload stays open; text extracted from an erased line loses its spaces, rendering unchanged), DOCX conversion, Gotenberg client (own multipart body, typed errors), role → submitter mapping, API `documents[]`/`fields[]` normalisation, HTML field elements → hidden tags |
| `spec/fixtures/ccn/fieldtags.pdf` (+ `build_fieldtags_pdf.rb`), `fieldtags-encrypted.pdf` | two-page A4 fixture with the 8 documented tags (generated with HexaPDF); the same file AES-encrypted with user password `secret` |
| `app/controllers/api/ccn_templates_documents_controller.rb` | `Api::CcnTemplatesDocumentsController` (not `Api::Ccn::…`, which would shadow `::Ccn`): `pdf`/`docx`/`html`/`merge`/`update`, one rescue table → 422 `{error}` (FR-015), renders `Templates::SerializeForApi` |
| `lib/ccn/create_template_from_documents.rb`, `lib/ccn/html_documents.rb`, `lib/ccn/merge_templates.rb`, `lib/ccn/update_template_documents.rb` | template creation from resolved files (`external_id` upsert via `Templates::ReplaceAttachments`, explicit fields win on name clash, `flatten` → AcroForm widgets detected then baked in by `Ccn::TextTags`), HTML → markers → Chromium → PDF (`size`, header/footer), merge (blobs shared, fresh uuids via `Templates::Clone`, roles positional or by name), add/replace/remove/merge of a template's documents (`PdfUtils`-style merge with page offsets; images become a page via `Templates::ModifyDocuments.open_or_build_pdf`) |
| `spec/fixtures/ccn/acroform.pdf` | AcroForm test PDF (text + signature widgets) for the `flatten` spec |
| `spec/requests/ccn_templates_documents_spec.rb` | US3: the five operations end to end (Gotenberg stubbed with the tag fixture) |
| `app/controllers/api/ccn_submissions_documents_controller.rb`, `lib/ccn/create_submission_from_documents.rb`, `lib/ccn/ingestion_errors.rb`, `app/jobs/ccn_discard_transient_template_job.rb` | `Api::CcnSubmissionsDocumentsController < Api::SubmissionsController` (reuses upstream's private `create_submissions`; upstream filters are all `only: :create`): documents → saved *transient* template (`preferences.ccn_transient`, no webhook/reindex) → upstream creation → detach (snapshot `template_schema/fields/submitters` copied, schema documents re-pointed to the submission, `template_id = nil`, name defaults to the document name) → transient (archived from creation, so never listed) destroyed through a fresh instance (the creating one holds the submission in its loaded association); a destroy that fails is logged and retried by `CcnDiscardTransientTemplateJob`, never masking the request's own error. `merge_documents`, `position`, `embed_src` per submitter; `template_ids`/`variables`/`emails`/`submission`/`submissions[]` (several submissions per request, also next to `submitters[]`) → 422 not supported yet. `position` is a 0-based index into the final order. No DB transaction spans downloads/conversions/uploads (as upstream): a failure destroys the transient template, which cascades to a half-built submission and purges the blobs. `Ccn::IngestionErrors` = the shared rescue table (422 `{error}`) of both API controllers |
| `spec/requests/ccn_submissions_documents_spec.rb` | US4: template-less submission, sign-through `/s/:slug` with Sidekiq inline (result documents generated without a template), merge/position, send_email, 422s |
| `config/initializers/zz_ccn_dentaku.rb` | `to_prepare` block that prepends `Ccn::BoundedExponentiation` / `Ccn::BoundedShift` into dentaku 4.x `Exponentiation` / `BitwiseShiftLeft` / `BitwiseShiftRight` |
| `lib/ccn/formula_bounds.rb`, `lib/ccn/bounded_exponentiation.rb`, `lib/ccn/bounded_shift.rb`, `lib/ccn/formula_out_of_range.rb`, `lib/ccn/formula_not_a_number.rb` | operands of `^`, `<<`, `>>` bounded at evaluation time (base ≤ 100 digits, |exponent| ≤ 1000, base digits × |exponent| ≤ 40 000 or 15 000 for a negative exponent, shift ≤ 64; numeric string literals coerced first) → `Ccn::FormulaOutOfRange` / `Ccn::FormulaNotANumber`, turned into HTTP 422 by `Submitters::SubmitValues` |
| `config/locales/ccn/ccn.yml` | fork strings (en, fr) |
| `app/views/shared/_ccn_not_yet.html.erb` | neutral "not available on this instance yet" notice |
| `spec/requests/ccn_stage1_spec.rb` | Stage 1 gate: switches on, no upsell strings on reachable pages, attribution + source link, formulas/conditions on completion |
| `spec/requests/openapi_contract_spec.rb` + `spec/support/openapi_contract.rb` | contract test: every operation of `docs/openapi.json` is routable except the pinned PENDING list, and each implemented operation's real response matches its declared schema (dependency-free validator) |
| `lib/ccn/admin_errors.rb`, `lib/ccn/mcp_tool_errors.rb` | Stage 3: `Ccn::AdminInvalid` + the rescue table of the admin controllers (422 `{error}`, 404 for a missing/foreign record); the MCP counterpart renders the same messages as tool errors (`isError: true`) via `Ccn::IngestionErrors.message_for` |
| `lib/ccn/manage_users.rb`, `lib/ccn/manage_webhooks.rb`, `lib/ccn/manage_account_configs.rb`, `lib/ccn/template_preferences.rb`, `lib/ccn/manage_folders.rb`, `lib/ccn/template_version_restore.rb`, `lib/ccn/detect_template_fields.rb` | Stage 3 services shared by REST and MCP: the UI controllers' rules as functions over hashes (users: status filters, reactivation, self guards, reset throttle; webhooks: `WebhookUrl::EVENTS` validation, write-only secret + reveal, deliveries, resend/test jobs; account configs: typed allow-list = `AccountConfigsController::ALLOWED_KEYS` ∪ `PersonalizationSettingsController::ALLOWED_KEYS` ∪ bcc/reminders, `EncryptedConfig` unknown by construction; template preferences: the `TemplatesPreferencesController` allow-list, `null` deletes; folders: two levels, default immutable, archive empty only; versions: restore after a pre-snapshot, refused when documents are gone; detection: synchronous JSON over `Templates::DetectFields`, 30-page cap, `apply` skips overlaps) |
| `app/controllers/api/ccn_users_controller.rb`, `ccn_webhooks_controller.rb`, `ccn_account_configs_controller.rb`, `ccn_template_folders_controller.rb`, `ccn_template_versions_controller.rb`, `ccn_template_detect_fields_controller.rb` | Stage 3 `/api/ccn/...` controllers: explicit `authorize!` per action (no `load_and_authorize_resource`), records loaded through `current_account`, upstream pagination |
| `app/controllers/mcp/ccn_tool_controller.rb` + `ccn_*_controller.rb` (8) | Stage 3 MCP tools: `SCHEMA` + `call`, indifferent arguments, template/submission summaries; ingestion tools reuse the Stage 2 modules (`Ccn::CreateSubmissionFromDocuments.after_create` and `Ccn::MergeTemplates.find_templates` moved out of the REST controllers for that) |
| `docs/openapi-ccn.json` | Stage 3: the fork's 29 operations (OpenAPI 3.1, upstream's style, `x-ccn-extends` on `PUT /templates/{id}`); generated by a one-off script, hand-editable. Stage 4 brings it to 34 (reminders ×2, account logo ×3) and adds `x-ccn-role-note` wherever an editor's or a viewer's token is answered differently |
| `lib/ccn/reminders.rb`, `app/mailers/ccn_submitter_reminder_mailer.rb` (+ its view), `app/jobs/ccn_send_submitter_reminders_job.rb`, `config/initializers/zz_ccn_reminders.rb`, `app/controllers/api/ccn_reminders_controller.rb` | Stage 4, US1 — signer reminders: the due rule (stage = reminders already sent + 1, due at `sent_at + duration`, never sent more than 7 days late, one stage per signer per run, `CCN_REMINDERS_MAX_PER_RUN` oldest first) with a Redis run lock; a mailer mirroring `SubmitterMailer#invitation_email` (subject/body: template preference → account config → default) that records a `send_reminder_email` event per delivery; the Sidekiq job and its `sidekiq-cron` entry, registered only when `CCN_REMINDERS_ENABLED=true` — through `Sidekiq.configure_server` + `on(:startup)`, **never** `Sidekiq.server?`, which is false in this image because Sidekiq runs embedded in Puma (that guard shipped in `3.2.4-ccn.5` and meant nothing was ever scheduled; fixed in `.6`); `GET /api/ccn/reminders/due` and `POST /api/ccn/reminders/run` (`dry_run`), administrators only and deliberately *not* an MCP tool (research D6) |
| `lib/ccn/account_logo.rb`, `app/controllers/api/ccn_account_logo_controller.rb`, `app/controllers/ccn_account_logos_controller.rb`, `app/views/shared/_ccn_brand_logo.html.erb`, `app/views/shared/_ccn_mailer_logo.html.erb` | Stage 4, US2 — the account logo: `has_one_attached :logo` with a validation that checks the declared type *and* the magic bytes with no filename hint (an SVG renamed `.png` reads as `application/xml` and is refused — scripted content in an e-mail client), 2 MB, PNG/JPEG/WebP; the attachment is named `logo` because both blob proxies already serve that name without a session, which is what lets an inbox fetch it. The API takes base64/data URI/https URL (never multipart: `ApiPathConsiderJsonMiddleware` reads every `/api` request as JSON), the settings page takes the file. The two render partials fall back to the DocuSeal mark, and the mailer one renders nothing when the instance has no absolute URL — a missing logo, never a broken image |
| `lib/ccn/submissions_lists.rb`, `app/controllers/ccn_submissions_lists_controller.rb`, `app/views/ccn_submissions_lists/preview.html.erb` | Stage 4, US4 — the Upload list tab: a CSV or XLSX of signers becomes one submission per row through `Submissions.create_from_submitters`, the same path `POST /api/submissions` takes. Two steps (preview, then confirm) with the rows carried in a signed payload rather than a re-upload; the whole file is refused when any row is wrong; the declared uncompressed size of an .xlsx is checked before rubyXL is asked to parse it |
| `lib/ccn/last_admin_guard.rb`, `lib/ccn/users_controller_guard.rb` | Stage 4, US3 — roles: a `User` validation refusing any save that would leave the account without an active, non-`integration` administrator (a model validation because users are demoted and archived from two controllers, the API, the MCP tool and the console), and the one-line `rescue_from` that turns it into a flash alert in `UsersController#destroy`, which archives with `update!` |
| `spec/lib/ability_spec.rb`, `spec/lib/ccn/last_admin_guard_spec.rb`, `spec/lib/ccn/account_logo_spec.rb`, `spec/lib/ccn/reminders_spec.rb`, `spec/mailers/ccn_submitter_reminder_mailer_spec.rb`, `spec/jobs/ccn_send_submitter_reminders_job_spec.rb`, `spec/jobs/ccn_send_submitter_reminders_job_scheduling_spec.rb` (the registration itself, with `Sidekiq.server?` stubbed false — the condition `.5` got wrong), `spec/requests/ccn_reminders_spec.rb`, `ccn_account_logo_spec.rb`, `ccn_roles_spec.rb`, `ccn_template_reminder_preferences_spec.rb`, `spec/support/fake_reminder_redis.rb` | Stage 4 specs (CI has Postgres but no Redis, hence the fake) |
| `spec/requests/ccn_users_spec.rb`, `ccn_webhooks_spec.rb`, `ccn_account_configs_spec.rb`, `ccn_template_admin_spec.rb`, `ccn_mcp_spec.rb`, `ccn_openapi_contract_spec.rb`, `spec/lib/ccn/manage_account_configs_spec.rb`, `spec/lib/ccn/template_preferences_spec.rb` | Stage 3 specs (MCP had no upstream tests) |
